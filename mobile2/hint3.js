function entrypoint(key, ciphertext_bytes) {
   Log(path_ciphertext + ":entrypoint()");

    titleView.setText("Key Three Hints");
    textView.setText(
    "Unlock KEY3 with the right text message"
    );

    return(true)
}
function verify_unlocked() {
    return(true);
}
